### Important things to note:
- Trace previous value
- One counter that only gets incremented if the conditons meets and updates the [i] value.
- nums.length = <number> deletes the rest of the numbers after the length
- Rotating in reverse
- Set for unique elements
- Merge sort algorithm
- Sliding window
- Map for frequency counts
- Hash map + prefix sum algorithm (two sum)